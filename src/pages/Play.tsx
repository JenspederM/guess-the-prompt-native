import React from 'react';
import {Text} from 'react-native-paper';
import {Container} from '../components/Container';

const Play = () => {
  return (
    <Container showBackButton showSettings>
      <Text>Play</Text>
    </Container>
  );
};

export default Play;
